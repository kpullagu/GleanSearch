version: 1.0
runtime: python311
build:
  commands:
    build:
      - python3.11 -m ensurepip --upgrade
      - python3.11 -m pip install -r requirements.txt
run:
  command: streamlit run glean_search_app.py